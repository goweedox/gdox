<fieldset>
	<% if !f.object.new_record? %>
		  <%= image_tag f.object.data.url %>
	<% end %>

	<%= f.file_field :data %><br/>
	<%= f.submit "Upload" %>
	<%= delete_screenshot(f) %>
</fieldset>
</div>
